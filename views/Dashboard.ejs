<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard - Test Bank System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<style>
.navbar.navbar-expand-lg.navbar-dark {
    background: linear-gradient(135deg, #001a8f 0%, #d4cc26 100%) !important;
}
</style>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-info">
        <div class="container">
            <a class="navbar-brand" href="#">
                <i class="fas fa-graduation-cap me-2"></i>Test Bank System
            </a>
            <div class="navbar-nav ms-auto">
                <span class="navbar-text me-3">
                    Welcome, Student <%= user.fullName %>
                </span>
                <form action="/auth/logout" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-outline-light btn-sm">
                        <i class="fas fa-sign-out-alt me-1"></i>Logout
                    </button>
                </form>
            </div>
        </div>
    </nav>
    <div class="container mt-4">
        <h1>Student Dashboard</h1>
        <p>This is the student dashboard. Content coming soon...</p>
        
        <!-- Quick Info Card -->
        <div class="row mt-4">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Student Information</h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Student ID:</strong> <%= user.userID %></p>
                        <p><strong>Name:</strong> <%= user.fullName %></p>
                        <p><strong>Email:</strong> <%= user.email %></p>
                        <% if (user.course) { %>
                            <p><strong>Course:</strong> <%= user.course %></p>
                        <% } %>
                        <% if (user.section) { %>
                            <p><strong>Section:</strong> <%= user.section %></p>
                        <% } %>
                        <% if (user.yearLevel) { %>
                            <p><strong>Year Level:</strong> <%= user.yearLevel %></p>
                        <% } %>
                        <% if (user.campus) { %>
                            <p><strong>Campus:</strong> <%= user.campus %></p>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Quick Actions</h5>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/student/tests" class="btn btn-primary">
                                <i class="fas fa-file-alt me-2"></i>Take Tests
                            </a>
                            <a href="/student/results" class="btn btn-success">
                                <i class="fas fa-chart-line me-2"></i>View Results
                            </a>
                            <a href="/student/account" class="btn btn-warning">
                                <i class="fas fa-user-cog me-2"></i>My Account
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>